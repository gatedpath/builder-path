# From the public registry, in an empty folder: 0.2.0 on 19 September 2026

Run the evening the owner published 0.2.0 of all nine packages, with a cleared npx cache, against
registry.npmjs.org. Node v24.20.0, Foundry 1.7.1, the owner's Mac. 43 s end to end.

| Step | Result |
|---|---|
| `npm view <name> version`, all nine | `0.2.0` each |
| `npm create redbelly-dapp@latest my-app -- --yes` | project written; `package.json` says scaffolder `0.2.0` |
| `npm run doctor` on the bare scaffold | `ok: every required check passed (2 warnings, optional)` |
| `npm install`, `npm run contracts:install`, `npm run test` | `68 tests passed, 0 failed, 0 skipped` |
| the shipped `contracts/src/GatedERC20.sol` | contains `revert ZeroParty()`: a forced transfer cannot mint |
| the shipped `contracts/script/RedbellyDeployScript.sol` | contains the `chain id mismatch` refusal |
| `npm run web:build` | `Compiled successfully`; `GatedAction.tsx` passes `chainId: chain.id` |
| the scaffold's `CLAUDE.md` | no `npx redbelly-` anywhere; prints `npx -y @gatedpath/agent-rules` |

The publish itself took four runs of `scripts/publish/first-publish.sh --publish`, each stopped safely
by the script's own tarball test: npm took from forty seconds to more than three minutes to serve a
sibling package published a moment earlier, so the next package's tarball would not install yet. Each
re-run skipped what was live. Nothing half-published, no file of hers left changed. The script's wait
is now three minutes; on the evidence of this run it should be longer, or the script should simply
be re-run.
