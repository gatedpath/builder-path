{
  "name": "__PROJECT_NAME__-contracts",
  "private": true,
  "description": "OpenZeppelin comes from npm at a pinned version (the method packages/receptor-mock chose); forge-std comes from `forge install --no-git` at a pinned tag; receptor-mock is vendored in lib/.",
  "scripts": {
    "install:forge-std": "forge install foundry-rs/forge-std@v1.16.2 --no-git"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "5.6.1"
  }
}
