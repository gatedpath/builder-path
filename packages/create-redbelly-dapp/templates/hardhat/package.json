{
  "name": "__PROJECT_NAME__-hardhat",
  "private": true,
  "type": "module",
  "description": "Hardhat 3 view of the same contracts, same solc 0.8.30 and Prague pins. src/ and lib/ are links to ../contracts.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test solidity",
    "keystore:set": "hardhat keystore set REDBELLY_DEPLOYER_KEY",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network redbellyTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network redbellyMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "5.0.7",
    "@openzeppelin/contracts": "5.6.1",
    "hardhat": "3.16.0",
    "typescript": "5.9.3",
    "viem": "2.56.3"
  }
}
