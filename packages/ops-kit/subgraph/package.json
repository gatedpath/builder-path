{
  "name": "gated-erc20-subgraph",
  "private": true,
  "version": "1.0.0",
  "description": "Goldsky subgraph template for a contract-kit GatedERC20 on Redbelly (151 or 153). Needs Redbelly's approval on Goldsky before it can be deployed.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy gated-erc20/1.0.0 --path ."
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  }
}
